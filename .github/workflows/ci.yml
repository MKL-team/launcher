name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  build-test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            artifact_path: build/bin/mkl_app
          - os: windows-2022
            artifact_path: build/bin/mkl_app.exe
          - os: macos-14
            artifact_path: build/bin/mkl_app.app/**
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.8.2'
          cache: true

      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 libegl1 libxkbcommon0 libdbus-1-3 ninja-build

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DMKL_BUILD_TESTS=ON

      - name: Build
        run: cmake --build build --config Release --parallel 2

      - name: Run tests (CTest)
        run: ctest --test-dir build -C Release --output-on-failure

      - name: Upload app artifact
        uses: actions/upload-artifact@v4
        with:
          name: mkl-app-${{ matrix.os }}
          if-no-files-found: error
          path: ${{ matrix.artifact_path }}
