# MKL 项目进度与计划（最小化跟踪）

> 更新：R2 架构切换后
> 本文件是项目的**最小进度跟踪 + 文档索引**。计划细节以规格书为准。

## 一、当前架构（一句话）

**主要 C（核心引擎）+ 中量 Rust（下载/认证/自更新）+ 少量汇编（SHA SIMD）+ C++ 胶水 + 5 套平台原生 UI**（详见 `MKL_开发规格书_R2.md`）。

## 二、里程碑进度

| 里程碑 | 内容 | 状态 |
| --- | --- | --- |
| R2-S1 核心骨架 | C 基础层 5 模块 + C 测试 + Rust 占位 + 双工具链 CI | 🟡 进行中（CI 验证中） |
| R2-S2 数据层 | C 账户存储（SQLite+加密）、配置管理 | ⬜ 未开始 |
| R2-S3 核心服务 | C 版本/安装/Java/启动/模组；Rust 下载/认证/自更新 + FFI | ⬜ 未开始 |
| R2-S4 Windows 壳 | Win32 UI + C++ 胶水，6 页面 | ⬜ 未开始 |
| R2-S5 macOS/Linux 壳 | AppKit / GTK3 | ⬜ 未开始 |
| R2-S6 移动端 | Android(Kotlin)/iOS(Swift) 壳 + 触控 + TouchUI Mod | ⬜ 未开始 |
| R2-S7 插件与发布 | 插件系统（C+Rust 沙箱）、.mklplugin、官网、Release 全矩阵 | ⬜ 未开始 |

## 三、R2-S1 当前状态（细项）

- ✅ C 基础层模块已写：logger / platform_detector / file_utils / string_utils / json（含 SHA-1）
- ✅ C 单元测试 18 项 + CLI 冒烟自检（mkl_app --smoke-test）
- ✅ Rust 占位模块 mkl_core_rs（版本解析 + Java 需求映射）——三平台 cargo test 已绿
- 🟡 C 核心 CI：上次运行构建成功，但
  - ✅ 已修复：SHA-1 长度字节大端顺序 bug
  - 🕐 待定位：mkl_core_tests 段错误（已加逐测试 stderr 输出，等待新 CI 定位）
- 🕐 GitHub 首次 Release（v0.0.1-alpha，旧 Qt 骨架）已打包 Windows/macOS 产物，Ubuntu 打包中断——历史快照，不再维护

## 四、文档索引（已保存）

| 文档 | 用途 | 状态 |
| --- | --- | --- |
| docs/MKL_开发规格书_R2.md | **现行唯一规格**（R2 架构） | ✅ |
| docs/MKL_规格问题清单与修正建议.md | 40 项问题 + 决策记录（历史） | ✅ |
| docs/MKL_开发规格书.md | v0.0.1 整理版（已废弃） | 存档 |
| docs/MKL_开发规格书_修正版.md | v0.0.1 修正版（已废弃） | 存档 |
| README.md | 仓库说明 + 构建/测试方法 | ✅ |

## 五、当前已知问题

1. `mkl_core_tests` 段错误（待新 CI 日志定位具体测试）
2. `_POSIX_C_SOURCE` 警告已修（localtime_r/uname/dirent）
3. Windows 构建的截断警告（snprintf 缓冲区大小）——无害，可后续清理
4. Rust 占位 crate 仅含版本解析；下载/认证/自更新待 R2-S3 实现

## 六、下一步（按优先级）

1. 🔴 定位并修复 mkl_core_tests 段错误 → R2-S1 CI 全绿
2. 🟡 R2-S2：C 数据层（config_manager 先行，account_storage 带 SQLite+凭证垫片）
3. 🟢 R2-S3：C 服务层骨架 + Rust FFI 示例（mkl_dl_add_task 原型）
